<Window x:Class="ColorPickerTray.PickerOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True"
        WindowStyle="None"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#01000000"
        KeyDown="Window_KeyDown"
        MouseMove="Window_MouseMove"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseRightButtonDown="Window_MouseRightButtonDown">
    <Canvas Background="#01000000">
        <Border x:Name="OverlayBorder" Width="400" Height="420" CornerRadius="12" Background="#DD1E2F79" Padding="14" BorderBrush="#88FFFFFF" BorderThickness="1" MouseLeftButtonDown="Window_MouseLeftButtonDown" MouseRightButtonDown="Window_MouseRightButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left: Preview + magnifier + copy controls -->
                <StackPanel Grid.Column="0" Width="140" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <TextBlock Text="当前颜色" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" />
                    <Border Width="68" Height="68" CornerRadius="6" x:Name="ColorPreview" Margin="0,0,0,8" BorderBrush="White" BorderThickness="1"/>
                    <Border Width="100" Height="100" Background="Black" CornerRadius="8" ClipToBounds="True" Margin="0,0,0,12">
                        <Image x:Name="MagnifierImage" Stretch="Fill" />
                    </Border>
                    <TextBlock x:Name="RgbText" Foreground="#CCFFFFFF" FontSize="12" Margin="0,0,0,6" TextWrapping="Wrap" />
                    <TextBlock x:Name="HexText" Foreground="#CCFFFFFF" FontSize="12" Margin="0,0,0,14" TextWrapping="Wrap" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <TextBlock Text="复制格式：" Foreground="White" FontSize="11" VerticalAlignment="Center" />
                        <ComboBox x:Name="FormatChoice" Width="110" FontSize="11" Margin="6,0,0,0" />
                    </StackPanel>
                    <TextBlock x:Name="LockStatusText" Foreground="#00FFCC" FontSize="10" FontWeight="Bold" Margin="0,10,0,0" TextWrapping="Wrap" Text="按 Alt 锁定窗口（未锁定）"/>
                    <TextBlock Foreground="#DDDDFF" FontSize="10" TextWrapping="Wrap" Margin="0,4,0,0" Text="左键复制并继续取色，右键或 Esc 退出。"/>
                </StackPanel>

                <!-- Right: Hue wheel + HSV bars -->
                <StackPanel Grid.Column="1" Margin="18,0,0,0" VerticalAlignment="Top">
                    <Border Width="152" Height="152" Background="Black" CornerRadius="76" ClipToBounds="True" HorizontalAlignment="Center">
                        <Canvas x:Name="WheelCanvas" Width="152" Height="152">
                            <Image x:Name="HueRingImage" Width="152" Height="152" Stretch="Fill" RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True" />
                            <Image x:Name="SvSquareImage" Stretch="None" />
                            <Canvas x:Name="WheelPointerOverlay" Width="152" Height="152" IsHitTestVisible="False" />
                        </Canvas>
                    </Border>
                    <Grid Width="214" Margin="0,14,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="H 色相" Foreground="White" FontSize="12" Margin="0,0,0,2" />
                            <Border Width="154" Height="14" CornerRadius="3" Background="Black" Margin="0,0,0,2">
                                <Grid>
                                    <Image x:Name="HueBar" Stretch="Fill" />
                                    <Canvas x:Name="HueBarOverlay" IsHitTestVisible="False" />
                                </Grid>
                            </Border>
                        </StackPanel>
                        <TextBlock x:Name="HueValueText" Grid.Row="0" Grid.Column="1" Foreground="#DDDDFF" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" />

                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,10,0,0">
                            <TextBlock Text="S 饱和度" Foreground="White" FontSize="12" Margin="0,0,0,2" />
                            <Border Width="154" Height="14" CornerRadius="3" Background="Black" Margin="0,0,0,2">
                                <Grid>
                                    <Image x:Name="SatBar" Stretch="Fill" />
                                    <Canvas x:Name="SatBarOverlay" IsHitTestVisible="False" />
                                </Grid>
                            </Border>
                        </StackPanel>
                        <TextBlock x:Name="SatValueText" Grid.Row="1" Grid.Column="1" Foreground="#DDDDFF" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" />

                        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,10,0,0">
                            <TextBlock Text="V 亮度" Foreground="White" FontSize="12" Margin="0,0,0,2" />
                            <Border Width="154" Height="14" CornerRadius="3" Background="Black" Margin="0,0,0,2">
                                <Grid>
                                    <Image x:Name="ValBar" Stretch="Fill" />
                                    <Canvas x:Name="ValBarOverlay" IsHitTestVisible="False" />
                                </Grid>
                            </Border>
                        </StackPanel>
                        <TextBlock x:Name="ValValueText" Grid.Row="2" Grid.Column="1" Foreground="#DDDDFF" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" />
                    </Grid>

                    <!-- Mini swatch panel: 2 rows x 5 swatches -->
                    <WrapPanel x:Name="MiniSwatchPanel" Width="130" Margin="0,12,0,0" />
                </StackPanel>
            </Grid>
        </Border>
    </Canvas>
</Window>
