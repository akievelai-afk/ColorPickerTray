<Application x:Class="ColorPickerTray.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Startup="Application_Startup"
             Exit="Application_Exit">
    <Application.Resources>
        <SolidColorBrush x:Key="BaseBackground" Color="White" />
        <SolidColorBrush x:Key="BaseForeground" Color="Black" />
        <SolidColorBrush x:Key="AccentBrush" Color="LightSkyBlue" />
        <SolidColorBrush x:Key="OverlayPanelBrush" Color="#DD1E2F79" />
        <Style TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource BaseBackground}" />
            <Setter Property="Foreground" Value="{DynamicResource BaseForeground}" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource BaseForeground}" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="BorderBrush" Value="#FFB0B0B0" />
        </Style>
    </Application.Resources>
</Application>
